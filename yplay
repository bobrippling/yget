#!/bin/sh
[ $# -eq 1 ] || { echo "usage: $0 URL"; exit 1; }
yurl "$1" |awk '
/^url34/ {gsub("'\''",""); url=$2}
/^url5/ && !url {gsub("'\''",""); url=$2}
{print}
END { if (url) system("mplayer -prefer-ipv4 -cache 32768 -cache-min 1 -cache-seek-min 10 '\''" url "'\''") }'
